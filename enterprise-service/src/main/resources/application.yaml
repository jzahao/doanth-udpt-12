server:
  port: 8083
  servlet:
    context-path: /enterprise

spring:
  application:
    name: enterprise-service
  datasource:
    url: "jdbc:mysql://localhost:3306/enterprise_service"
    #    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  services:

